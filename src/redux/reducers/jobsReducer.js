/*
import {ZZZZZ} from "../actions";

const initialState = {
  content: [],
};

const xxxxx = (state = initialState, action) => {
  switch (action.type) {
    case ZZZZZ:
      return {
        ...state,
        content: action.payload,
      };
    default:
      return state;
  }
};

export default xxxxx;
*/